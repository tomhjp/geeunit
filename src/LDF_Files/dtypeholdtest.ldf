STARTFILE
DEVICES
    sw = SWITCH(0);
    clk = CLOCK(2);
    set = SWITCH (0);
    clear = SWITCH(0); 
    dtype = DTYPE;
END
CONNECTIONS
    sw -> dtype.DATA;
    clk -> dtype.CLK;
    set -> dtype.SET; 
    clear -> dtype.CLEAR;
END
MONITORS
    dtype.Q;
    clk; 
END
ENDFILE
